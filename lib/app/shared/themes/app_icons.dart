import 'package:tcc_bag_finder/app/shared/themes/app_dimensions.dart';
import 'package:tcc_bag_finder/app/shared/themes/app_colors.dart';
import 'package:flutter/material.dart';

class AppIconsPrimary {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get directionRunIcon => Icon(
        Icons.directions_run_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get contentPasteIcon => Icon(
        Icons.content_paste_search_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get airPlaneModeIcon => Icon(
        Icons.airplanemode_active_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get connectingAirportsIcon => Icon(
        Icons.connecting_airports,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get descriptionIcon => Icon(
        Icons.description,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );

  static Icon get notificationIcon => Icon(
        Icons.notifications,
        size: AppDimensions.iconMedium,
        color: AppColors.primary,
      );
}

class AppIconsSecondary {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get directionRunIcon => Icon(
        Icons.directions_run_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get contentPasteIcon => Icon(
        Icons.content_paste_search_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get airPlaneModeIcon => Icon(
        Icons.airplanemode_active_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get connectingAirportsIcon => Icon(
        Icons.connecting_airports,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get descriptionIcon => Icon(
        Icons.description,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );

  static Icon get notificationIcon => Icon(
        Icons.notifications,
        size: AppDimensions.iconMedium,
        color: AppColors.secondary,
      );
}

class AppIconsSecondaryGrey {
  static Icon get homeIcon => Icon(
        Icons.home,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get historyIcon => Icon(
        Icons.history,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get searchIcon => Icon(
        Icons.search,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get filterIcon => Icon(
        Icons.filter_list,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get messageIcon => Icon(
        Icons.message,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get emailIcon => Icon(
        Icons.email,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get passwordIcon => Icon(
        Icons.lock,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get personIcon => Icon(
        Icons.person,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get luggageIcon => Icon(
        Icons.luggage,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get idCardIcon => Icon(
        Icons.credit_card,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get calendarIcon => Icon(
        Icons.calendar_today,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get phoneIcon => Icon(
        Icons.phone,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get directionRunIcon => Icon(
        Icons.directions_run_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get contentPasteIcon => Icon(
        Icons.content_paste_search_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get airPlaneModeIcon => Icon(
        Icons.airplanemode_active_sharp,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get connectingAirportsIcon => Icon(
        Icons.connecting_airports,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get descriptionIcon => Icon(
        Icons.description,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );

  static Icon get notificationIcon => Icon(
        Icons.notifications,
        size: AppDimensions.iconMedium,
        color: AppColors.secondaryGrey,
      );
}
